#!/usr/bin/env ruby

# Allows you to require any file in lib
$: << File.join(File.dirname(__FILE__), '..', 'lib')

require 'contact'

my_contacts = Contact.new
my_contacts.begin
# p my_contacts.contacts
# p my_contacts.sort_by_first_name
# p my_contacts.current_contacts
# p my_contacts.sort_by_first_name
# # p my_contacts.current_contacts


# Require files here to experiment with them in an irb session

# require "irb"
# IRB.start(__FILE__)

#!/usr/bin/env ruby

# Allows you to require any file in lib
$: << File.join(File.dirname(__FILE__), '..', 'lib')

require 'contact'

new_person = Contact.new
new_person.create_new_entry
new_person.create_new_entry
new_person.display_contacts

# Require files here to experiment with them in an irb session

# require "irb"
# IRB.start(__FILE__)
